import React, { Fragment } from 'react';
import styled from 'styled-components';

const Footer = styled.footer`
  min-height: 150px;
`;

export default () => (
  <Footer>
    <h1>Footer</h1>
  </Footer>
);
